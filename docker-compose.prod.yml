version: '3.8'

services:
  web:
    image: pomopomo-web
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    environment:
      - NODE_ENV=production
    env_file:
      - .env.production
    networks:
      - app_network
    # Bind to localhost so Host Nginx can proxy to it
    ports:
      - "127.0.0.1:3000:3000"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  ws:
    image: pomopomo-ws
    build:
      context: .
      dockerfile: Dockerfile.ws
    restart: always
    environment:
      - NODE_ENV=production
    env_file:
      - .env.production
    networks:
      - app_network
    ports:
      - "127.0.0.1:3003:3001"
    depends_on:
      - redis
      - postgres

  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app_network
    # No ports exposed to host for security

  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    networks:
      - app_network
    # No ports exposed to host

  worker:
    image: pomopomo-worker
    build:
      context: .
      dockerfile: Dockerfile.worker
    restart: always
    env_file:
      - .env.production
    networks:
      - app_network
    depends_on:
      - redis
      - postgres

networks:
  app_network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
